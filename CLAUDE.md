# Our Session 開発原則（CLAUDE.md）

> このファイルは Claude Code への指示書です。実装・提案・レビューのすべての判断は、ここに書かれた原則に従ってください。迷ったら **第0条「判断の優先順位」** に立ち返ること。

-----

## 第0条：判断の優先順位（迷ったらこの順で決める）

機能・実装・コピーで判断に迷ったら、**上から順に**優先する。

1. **初心者・女性ユーザーが「怖い／恥ずかしい／面倒」を感じないこと**
2. **音源を起点に「会いたい」が生まれ、リアルセッションへ繋がること**
3. **開発速度（ソロ開発で破綻しないこと）**
4. **見た目の華やかさ・技術的な高度さ**

> 4 が 1〜3 と衝突したら、迷わず 4 を捨てる。ただし「質感へのこだわり」そのものが 2（会いたいが生まれる）を底上げする場合は、単なる装飾とは区別して判断する。詳細は §2「ビジュアルの温度」。

-----

## 1. プロダクトビジョン（1文）

**音源（MP3）を聴いて「この人と演奏したい」と思った瞬間を、スタジオでのリアルなセッションに繋げるマッチングサービス。**

- アプリ内で完結させない。最終ゴールは「スタジオのドアを開けること」。
- nana・session との違い：あちらは画面内で完結する非同期コラボ。Our Session は**会うための道具**。

-----

## 2. コアバリューとトーン・オブ・ボイス

### コアバリュー

**「下手でいい。好きでつながる。」** — 上手さの審査ではなく、好きを共有して会う相手を見つける場。

### トーン・オブ・ボイス（コピーを書くときの絶対ルール）

**Do（推奨）**

- 「スマホで録ったものでOK」「まず聴くだけでOK」のように**ハードルを下げる**
- 「あなたの音、届きました🎵」のように**一歩を肯定する**
- 「会えそうですね。場所、探しますか？」のように**そっと背中を押す**
- 絵文字は控えめに、温かみのある箇所のみ（🎵🎸 程度）

**Don't（禁止）**

- ❌ ユーザーに「下手ですみません」と**謝罪させる**前提のUI・プレースホルダー
- ❌ 「○時間以内に返信を」など**急かす**文言
- ❌ 「ランキング」「いいね数」「再生回数の競争」など**優劣を可視化**する表現
- ❌ 冷たい・事務的・命令的なトーン（「エラー」より「うまく送れませんでした」）

### コピー実例（プロトで確定済み・そのまま使うこと）

**完了系**:
- 投稿完了: 「あなたの音、届きました🎵 / 仲間が見つかったらお知らせします。」
- アンサー送信: 「あなたの音、届きました🎵 / 相手が聴いてくれたら、メッセージでつながれます。」
- プロフィール更新: 「プロフィールを更新しました」
- 認証完了(登録): 「ようこそ🎵 / まずは気になる人の音源を聴いてみよう。」
- 認証完了(ログイン): 「ようこそ🎵 / おかえりなさい。」
- ログアウト: 「ログアウトしました」

**通知系**:
- 新着アンサー: 「○○さんから音が届きました🎵」
- 承認時(チャット内システムメッセージ): 「○○さんと、会えそうですね🎵」
- 承認時(通知): 「○○さんと、会えそうですね🎵」

**エラー系**:
- 形式違い: 「いまはMP3だけ送れます。形式を確認してみてください。」
- サイズオーバー: 「ファイルが少し大きいようです（○MB）。5MB以下にしてみてください。」
- アップロード失敗: 「うまく送れませんでした / 電波の届く場所で、もう一度お試しください。」
- オフライン: 「音が遠くなりました / 電波の届く場所で、もう一度開いてみてね。下書きは消えていません。」

**通報・ブロック**:
- 通報送信完了: 「通報を受け付けました / 運営が確認のうえ、適切に対応します。」
- ブロック完了: 「○○さんをブロックしました / 視界から消えます。設定から解除できます。」

**フィードバックの表示方法**: ブラウザ標準の `alert()` ではなく、**画面下部のトースト**で表示。フローティングナビの少し上、3.8秒で自動消滅、×ボタンで手動クローズ。

-----

## 3. 技術的制約と守るべきこと

### スタック（MVP）

- **フロント**: Next.js (App Router) + TypeScript
- **スタイル**: Tailwind CSS（デザイントークンは後述の一元管理を厳守）
- **バックエンド**: Supabase（Postgres + Auth + Storage + Realtime）
- **デプロイ**: Vercel
- **音源再生**: HTML5 `<audio>` + 自前のシンプル再生バー（波形は Phase2）

### PWA 前提（MVPからPWAとして成立させる）

- `manifest.json` を用意し、ホーム画面インストールに対応（スマホファースト）
- Service Worker は**アプリシェル（UI骨格）のみキャッシュ**。音源ファイルは原則キャッシュしない（容量・著作権・鮮度の観点）
- オフライン時は「**音が遠くなりました**」の温かいフォールバック画面を表示。**白画面で落とさない**
- インストール促進バナーは初回オンボーディング直後ではなく、**2〜3回利用後**に控えめに出す（押し付けない）

### 将来方針

- React Native 移行を視野に入れるが **MVPはPWA優先**。
- そのため、**ビジネスロジック・データアクセスは UI から分離**しておく（移行時に再利用できる形に）。

### 守るべきこと

- 音源・個人情報は **Supabase RLS で必ず保護**。承認済みアンサー参加者以外はチャットを読めない。
- 環境変数・APIキーをコードに直書きしない。

-----

## 4. UI/UX原則（初心者心理の安心設計）

### 感情の階段を一段ずつ

ユーザーを急に投稿させない。「**聴く → こっそり気になる → ひとりじゃないと知る → 投稿**」の順で背中を押す。

### 必ず実装する安心装置

- **「練習中」フラグ**（デフォルトON）。下手を言葉で謝らなくていい免罪符。閲覧側UIでは「歓迎される印」として扱う（自己卑下にしない）。
  - 表示: プロフィールページ・マイページ・セッションカードのユーザー名横に「練習中」バッジ（絵文字なし、テキストのみ）
  - 色: 黄→オレンジのグラデーション（`#ffd884` → `#ffa964`、歓迎の温かさ）
- **公開範囲の明示**：投稿フォーム上部に安心ブロック（暖色アイコン + 「**足あとは残りません**」+ 補足文）を必ず1つ提示。**最大の恐怖を投稿前に消す**。
- **沈黙への配慮**：プレイヤー直下に「**いま○人が聴いています**」を表示（現在進行形、緑色の控えめなドット）。**無反応＝無価値にしない**。0人の場合は表示しない。
- **ハート保存（実装はブックマーク🔖アイコン）**：数字なし・足あと残らない・こっそり。
- **承認制 + ブロック・通報**：断りやすく、選べる。MVPから必須。詳細は次項。

### 通報・ブロック機能の仕様

**入口**:
- セッションカードの⋯メニュー(他人のカード)
- プロフィールページの⋯メニュー(他人のプロフィール)

**通報**:
- 6カテゴリ: スパム・宣伝 / 不適切な内容 / 誹謗中傷・嫌がらせ / 著作権侵害 / なりすまし / その他
- 補足コメント(任意、300字)
- シート上部に「**あなたが通報したことは相手には伝わりません**」を明示

**ブロック**:
- 確認ダイアログで以下を明示:
  - お互いの投稿やアンサーが見えなくなります
  - メッセージのやり取りもできなくなります
  - ブロックしたことは相手に通知されません
  - 設定からいつでも解除できます

### 数字の扱い

プロフィールに「投稿数/アンサー数/セッション数」のような統計数字は**置かない**。代わりにマイページに「**○○さんのプロフィールと似ている人**」スライダーを置く。

理由: 数字での競争・ステータス化を避け、**新しい出会いを促す指標**にする。リコメンドは「マイページのみ」(他人のプロフィールページには出さない)。スコアリングは「好きなアーティスト×3 + ジャンル×2 + 楽器×1 + エリア×1 + 練習中フラグ×1」で算出。

### レイアウト・操作

- スマホファースト。1スクリーンに 1〜1.5 枚のゆったりリズム（"散歩"感）。
- セッションカード：本体タップは無反応。**サムネ→プロフィール / ボタン→ドロワー**のみ反応。
- 音源プレイヤーの**真下にアンサーボタン**を近接配置（聴く→申し込むの熱を冷まさない）。
- 本文は折りたたみ（**3行プレビュー**）で視線を音源・アクションに集中させる。**本文が3行に収まる場合は「続きを読む」自体を出さない**（overflow検知で判定）。3行を超えるときのみ、3行目の末尾に「続きを読む」を表示してタップで展開、「閉じる」タップで折りたたみ。

### ビジュアルの温度

**なぜデザインにこだわるか**：Our Session は「誰でも歓迎」を掲げるが、それは「雑でいい」という意味ではない。音楽・演奏を本気で好きな人ほど、作り込まれていない場には集まらない。手を抜かない質感・色温度そのものが「ここは同じくらい本気の人がいる場所だ」というシグナルになり、**ユーザーの純度（本気度）を上げるフィルター**として機能する。見た目は第0条で優先順位4位だが、これは「1〜3を無視して装飾に凝る」こととは違う——**質の高い体験そのものが2「会いたいが生まれる」を底上げする**という位置づけで判断すること。

- Modern Dark をベースに、アクセントは**暖色のテラコッタ**（燻し銅・アンバー寄り）で「夜のスタジオの灯り」の温かさを表現。
- 冷たいネオン感・派手なグロー演出（box-shadow、発光する背景演出）は使わない。**フラットで落ち着いた質感**に温もりを込める。
- 背景・カードは単色。透過や発光に頼らず、色そのもの（テラコッタ×ダークブラウン）で温度を出す。

### デザイントークン

```
--bg: #101010                 /* ページ背景（単色、発光・透過演出なし） */
--card-solid: #171717         /* セッションカード等、不透明な塗り */
--border-solid: #222222       /* セッションカード等の枠線 */
--card: rgba(26,26,32,0.55)   /* チップ・シート等、半透明が必要な箇所のみ */
--card2: rgba(34,34,42,0.5)
--text: #f5f5f7  --text2: #9a9aa8  --text3: #5a5a66
--red: #b5593c                 /* メインアクセント(テラコッタ) */
--red2: #c8836d
--red-bg: rgba(181,89,60,0.10)
--red-border: rgba(181,89,60,0.32)
--accent-muted: #a47c70        /* 副次テキスト・アイコン（日付・保存/⋯・本文など） */
--accent-light: #d2b9ae        /* 強調テキスト（タイトル・タグ文字など） */
--on-accent: #101010           /* アクセント色の上に乗る文字・アイコン（再生ボタン内・送信ボタン文字） */
--tag-solid: #161618           /* タグチップの塗り */
--orange-lit: #e08a5a          /* フォーカスリング */
--audio-card: #251a16          /* 音源カード（録音/ファイル選択）の暖色背景 */
--notice-well: #2e1e19         /* 安心ブロックのアイコン円背景 */
--req: #8f2a38                 /* 必須ラベル用の濃い赤 */
```

**フォント**: Outfit (300-800) / 日本語: システム  
**アイコン**: 統一感のある手選定SVGアイコンセット（`components/icons/CustomIcons.tsx`）。塗りつぶし系グリフで統一し、色は上記トークン経由で指定する（Feather Iconsからの差し替え済み）  
**ロゴ**: `public/logo_oursession.png` の実画像を使用（CSSでのテキスト再現はしない）  
**カード**: セッションカードは不透明（`--card-solid`）・角丸12px・**box-shadowなし**。それ以外の半透明UI（チップ・シート等）は `backdrop-filter: blur(20px)` を使ってよい  
**ナビ**: 下から浮いた角丸ピル（フローティング）。背景は単色`--bg`+`--card-solid`の枠線（ぼかしなし）/ 返信マーク: ↳

### 選択チップの見せ方

楽器・ジャンル・好きなアーティスト等の選択チップは、選択時に**塗りつぶさない**:
- 背景: `rgba(181,89,60,0.04)`（ほぼ透明）
- ボーダー: 細いテラコッタ（`--red`）
- 文字: テラコッタ（`--red` または `--red2`）
- 文字weight: 600

理由: 赤系の塗りつぶしが画面の半分を占めると「叫ぶ」印象になる。線と文字だけでも選択感は十分。

### box-shadowを使わない

グロー・浮遊感を演出するbox-shadowは全面的に廃止し、フラットな質感で統一する。強調したい要素は色（`--red`系アクセント）とborder-radiusの非対称性（例: セッションアンサー送信ボタンの「左上・右下のみ角丸」）で表現する。

-----

## 5. コード品質基準

- **デザイントークンは `lib/design-tokens.ts`（または Tailwind config）に一元管理**。色・余白・フォントをコンポーネントに直書き禁止。
- **音源処理は必ず `lib/storage.ts` 経由**。Supabase Storage SDK を直接叩かない（将来のCDN移行のため抽象化）。
- TypeScript の型を必ず付ける。`any` は原則禁止。
- コンポーネントは単一責任。`SessionCard` / `AudioPlayer` / `PracticeBadge` / `AnswerDrawer` / `FloatingNav` / `Toast` / `UploadState` / `Recorder` / `Wordmark` を分離。
- データアクセス層を UI から分離（RN移行・保守のため）。
- ファイル命名規則（音源）: `{user_id}/{session_id}/{uuid}.mp3` で衝突回避・権限境界を明確化。
- カード削除時に音源を消すクリーンアップを必ず実装（**孤児ファイルを残さない**）。

-----

## 6. Plan Mode 使用ルール

「実装→提示→ダメなら戻す」スタイルを尊重する。以下は**着手前に必ず Plan Mode で設計を提示し、承認を得てから実装**する。

**Plan Mode 必須**

- DBスキーマの新規作成・変更
- RLSポリシーの設計・変更
- 音源パイプライン（アップロード／保存／再生）に触れる変更
- 画面・ドロワーの新規追加、ナビ構造の変更
- 認証フロー

**Plan Mode 不要（直接実装してよい）**

- マイクロコピーの調整、色・余白などパラメータの微修正
- 既存コンポーネント内の軽微なスタイル変更

**進め方**

- 大きな変更は「実装→提示」、細部は「パラメータ提案」。
- スクショでの感覚的フィードバック（「いい感じ」「イマイチ」）を前提に、即修正のループを回す。
- 最初の1ヶ月は基本的に Plan Mode を ON にしておく（慣れてきたら判断で外す）。

-----

## 7. 音源処理に関する特別ルール（このアプリの心臓部）

> 音源が「確実に上がって、誰のスマホでも鳴る」ことに成否の9割がかかっている。最優先で堅牢に作る。

### 基本制限

- **形式**: `audio/mpeg`（MP3）に限定。それ以外は投稿前に弾き、温かい文言で案内。
- **サイズ・長さ制限**: 5MB / 90秒。クライアント側で投稿前にチェック。  
  理由: 1) 転送コスト削減（Supabase無料枠 = 月5GB）、2) nanaと同じ業界標準、3) 初心者の「長く録らないと」プレッシャーを下げる。
- **コスト管理**: 無料枠の帯域・容量を意識。テスト音源は使い捨て、定期削除。

### アップロードUIのステートマシン（プロトで確定）

```
[idle] → ファイル選択 or 録音
   ↓
[validating] → 形式チェック → サイズチェック
   ↓ NG: [error] 温かい文言 + 「もう一度試す」「別のファイル」ボタン
   ↓ OK
[uploading] → 進捗バー 0% → 100%
   ↓ 失敗: [error]
   ↓ 成功
[success] → 緑チェック + 波形プレイヤー
```

**エラー状態のUI**:
- アンバー(琥珀)色のアラートアイコン（赤じゃない = 攻撃的じゃない）
- 「うまく送れませんでした」のような温かい文言（「エラー」と書かない）
- ボタン2つ: 「別のファイル」「もう一度試す」(リトライがアンバー寄り強調色)

### その場録音フロー（プロトで確定）

「ファイル選択」と「その場録音」を**並列で見せる**:
- 録音ボタン(主役、`--audio-card`の暖色カードに円形アイコンウェル + テラコッタのマイクアイコン)が上
- 「または」のセパレーター(罫線なし、テキストのみ)
- 「ファイルから選ぶ」(代替手段)が下

**録音ボタンタップ後のフロー**:
1. **3秒カウントダウン**（72pxの大きな数字 + ポップアニメ、いきなり録音を始めない）
2. 最後は「スタート」(緑)に変わってから0.6秒後に録音開始
3. **録音中**: 赤いパルスドット + リアルタイム波形(32本) + タイマー(0:00/1:30) + 「やめる」「停止」ボタン
4. **90秒で自動停止**
5. **プレビュー**: 再生して聴き直せる + 「もう一度」「これで送る」
6. 「これで送る」で既存のアップロードパイプラインに合流

### モバイル録音の技術的注意点

- **MediaRecorder API** でその場録音 → Blob → アップロード
- iOS Safari は `audio/mp4`、Chrome は `audio/webm` で録音（MP3を直接録音できるブラウザはない）
- **iOS Safari の形式差異を吸収する薄いラッパ**を `lib/storage.ts` に用意
- **方針**: 各ブラウザの形式のまま保存し、サーバー側で MP3 変換は行わない（変換コスト削減、実装シンプル化）
- マイク権限拒否時のフォールバック必須（「設定でマイクを許可してください」の案内）
- HTTPSが必須（`localhost` または `https://`）

### 配信

- 署名付きURL + キャッシュ
- バズ時に Cloudflare R2 等へ逃がせるよう Storage 層を抽象化
- オフライン時: 音源は Service Worker でキャッシュしない（前述）

### コスト構造のメモ（将来の判断のため）

**Supabase 料金が発生するポイント**:
1. ストレージ容量（MP3保存）— 比較的安い
2. **転送（再生のダウンロード）— 最大の課金ポイント**
3. DB容量（テキスト）— 安い
4. Auth・Realtime — MVP規模では無料枠で十分

**抑制の方針**:
- 90秒/5MB制限は転送費を直接削減する
- 音源変換は「しない」が最もシンプル（各ブラウザの形式のまま保存）
- 成熟期に Cloudflare R2 等への移行を視野（Storage層の抽象化が大事）
- 削除のクリーンアップ徹底（孤児ファイル防止 + ストレージ節約）

**規模感の目安**:
- 〜1000人: 月$25(Pro)程度
- 〜1万人: 月$30〜100程度
- 〜10万人: CDN移行必須、月数万円〜十数万円

-----

## 8. オンボーディング（登録直後）

登録直後の最小ウィザード(3ステップ + ウェルカム)。**ログインの場合はオンボードを起動しない**。

### ステップ構成

1. **ニックネーム**: 「あなたを何て呼べばいい?」「本名でなくて大丈夫。後からいつでも変えられます。」
2. **楽器**: 「どんな楽器をやってる?」 + 練習中トグル(**デフォルトON**)
3. **エリア**: 「どのエリアでセッションする?」(47都道府県グリッド、1選択)
4. **ウェルカム**: 「ようこそ、○○さん 🎵 / まずは気になる人の音源を聴いてみよう。」+「はじめる」

### UI仕様

- 上部に進行バー(3つの細い線)
- 戻るボタン(ステップ2,3で表示、1とウェルカムでは非表示)
- 「次へ」は条件を満たすまで非活性(灰色 → 赤に変化)
- 入力された情報は本実装で `profiles` テーブルに保存

### 任意項目は後で

アバター・自己紹介・好きなジャンル・好きなアーティスト・好きな曲・SNSはプロフィール編集ドロワーで後から設定。「**最小ウィザード + 後から育てる**」設計。

-----

## 付録：データモデル（参照用）

```
profiles ──< sessions ──< answers
   │            │            │
   │            └──< messages（カード単位チャット）
   │
   └──< saves（ハート保存・数字非表示）

studios（スタジオマスタ・静的）

sessions(id, author_id, title, body, audio_url, is_practice, tags[], created_at)
answers(id, session_id, sender_id, audio_url, message,
        status['pending'|'approved'|'declined'], created_at)
messages(id, session_id, sender_id, body, created_at)  ※承認済み参加者のみ(RLS)
saves(user_id, session_id)  ※複合PK
```

### プロフィール拡張フィールド（オンボード/編集で扱う）

```
profiles {
  id, nickname, area, is_practice (default true),
  instruments[], genres[], favorite_artists[], favorite_tracks[],
  bio, avatar_url, sns_links,
  created_at, updated_at
}
```

-----

## 付録：カードのライフサイクル（実装フェーズで決定）

セッションカードの「一生」（作成 → タイムライン掲載 → 終了/削除）の設計は、実際のユーザー行動を見てから決める。**MVP時点では削除機能のみ**。

### 後で検討する論点

- 自動消滅（タイムラインから時間経過で外す）を入れるか / 何日後か
- 「募集終了」ボタン（投稿者の能動操作）
- 「セッション成立」フラグ（リアル世界での結果を記録）
- 終わったカードの保管場所（投稿者のマイページ「過去のカード」など）
- アンサー送った側の体験（承認待ち放置への配慮）
- チャットルームのアーカイブ
- スタジオ予約連携（Step2）時に自動的に「成立済み」にする可能性

### 判断基準

- 第0条1: 初心者が「何もしなくても綺麗になる」を実現できる設計か
- 第0条2: 「スタジオのドアを開けた」記録としての価値があるか

-----

## 付録：実装の優先順位（推奨）

1. プロジェクトセットアップ + デザインシステム（トークン、ベースコンポーネント）
2. 認証フロー + オンボーディング
3. タイムライン（セッションカード表示、波形プレイヤー）
4. **音源アップロード（§7 特別ルール、最優先で堅牢に）**
5. 投稿フロー
6. アンサーフロー + 承認
7. **メッセージ機能（チャットルーム、Realtime + RLS）**
8. プロフィール + 保存機能 + 「似ている人」スライダー
9. 通報・ブロック（既存機能の上に被せる）
10. PWA化（manifest + Service Worker + オフライン画面）

-----

**最後に**：このアプリは「上手さを競う場所ではない」。すべての実装は、楽器を始めて半年の人が**怖がらずに最初の一歩を踏み出せるか**で判断する。はるお
